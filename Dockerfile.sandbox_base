# AI_sandbox/Dockerfile.sandbox_base
# Catalogに登録するイメージの例 (Python)
FROM python:3.10-slim-bookworm

# 必要なツールやライブラリを追加する場合
# RUN pip install requests pandas numpy # 必要に応じて

WORKDIR /app

# エントリポイントは、LLMが提供するコードを直接実行するように設定
# コンテナが起動時に何もせず、外部からコマンドを受け付けるようにする
ENTRYPOINT ["bash", "-c"]
# 例えば、CMD ["sleep", "infinity"] としておき、
# docker run -it --rm my-sandbox bash -c "python -c 'print(\"hello\")'" のようにコマンドを渡す
# SandboxManagerServiceが run_container で command を渡すため、ENTRYPOINTはシンプルで良い。